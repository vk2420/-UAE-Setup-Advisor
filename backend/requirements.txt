# Runtime dependencies for the API + LLM layer.
# NOTE: the deterministic engine itself needs NONE of these — it is stdlib-only.
# These are only for the web/API surface and the optional LLM explanation.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
anthropic>=0.40   # optional at runtime; only used when ANTHROPIC_API_KEY is set
